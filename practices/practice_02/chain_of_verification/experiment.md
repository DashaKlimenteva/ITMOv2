# Chain of Verification

- Проверяемый черновик или утверждение: `practices/practice_01/tests_load.md` — кейсы L1, L2, L3. Цель: подтвердить источниками, добавить числовые пороги и критерии, исключить неподтверждённые сценарии.

## Запрос на проверку

Проверить подтверждённость кейсов L1-L3 правилами `practices/practice_01/CASE.md` (API-1, REL-1, QA-1, OBS-1) и строками `practices/practice_01/TRAINING_PR.diff → app/api.py:35-38; app/review_service.py:19-22`. Уточнить числовые пороги и критерии провала. Исключить неподтверждённые сценарии. Не использовать знания вне источников; не логировать содержимое diff (OBS-1).
## Вопросы проверки и evidence

| Вопрос | Источник или проверка | Результат |
|---|---|---|
| L1 — Есть ли правило о максимальной длине `diff` и требование HTTP 413 при превышении? | `CASE.md → API-1`; подтверждение релевантности эндпоинта: `TRAINING_PR.diff → app/api.py:35-38` | Подтверждено: порог 20000 символов, при превышении ожидать 413 |
| L2 — Есть ли правило/метрика о параллельных запросах, ≥99% 200 и допустимой латентности? | Проверка `CASE.md` (нет правил о параллельности/99% 200); `problem.md → Метрики` (нет порога для параллельности/латентности); `TRAINING_PR.diff` (не задаёт такие ограничения) | Не подтверждено: отсутствуют источники для 99% 200 и латентности |
| L3 — Есть ли правило о таймауте внешнего LLM и требование контролируемого ответа? | `CASE.md → REL-1` (таймаут 10 секунд, контролируемая ошибка); подтверждение вызова LLM: `TRAINING_PR.diff → app/review_service.py:19-22` | Подтверждено: таймаут 10 секунд; ожидать контролируемый ответ без 5xx |

## Исправленный результат

Создан `practices/practice_02/chain_of_verification/tests_load_improved.md` с подтверждёнными кейсами L1 и L3; L2 не перенесён как неподтверждённый.
## Что изменили в исходном артефакте

- Файл и раздел: `practices/practice_02/chain_of_verification/tests_load_improved.md` — весь документ.
- Изменение: добавлены подтверждённые пороги (20000 символов по `API-1`; 10 секунд по `REL-1`), инструменты, критерии провала, связь с метриками и evidence на кейс; учтён запрет логирования содержимого по `OBS-1`.
- Что отклонили: L2 (параллельные запросы, ≥99% 200, латентность) — отсутствуют подтверждающие правила/пороги в `CASE.md` и `problem.md`.

Скрытые рассуждения модели не сохраняйте; нужны только вопросы, evidence и исправленный результат.
